{
  "name": "@parakh/legal-metrology-rules-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic, evidence-aware Legal Metrology (Packaged Commodities) Rules engine for PARAKH.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/tests/*.test.js",
    "check": "npm run build && npm test",
    "start": "npm run build && node dist/src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.15.5",
    "typescript": "^5.6.0"
  }
}
