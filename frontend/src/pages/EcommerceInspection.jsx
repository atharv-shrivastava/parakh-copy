import {useState} from "react";import {apiFetch} from "../lib/auth";export default function EcommerceInspection(){const[url,setUrl]=useState("");const[data,setData]=useState(null);const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function inspect(e){e.preventDefault();setBusy(true);setMsg("");try{const r=await apiFetch("http://localhost:5000/api/products/ecommerce/analyze-url",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url})});const d=await r.json();if(!r.ok)throw Error(d.error||"Inspection failed");setData(d)}catch(e){setMsg(e.message)}finally{setBusy(false)}}return <div className="products-page"><div className="page-header"><p className="eyebrow">E-COMMERCE INSPECTION</p><h1>Inspect online listing</h1><p>Provide a public product URL. PARAKH extracts listing metadata and can send public product images through the shared OCR service.</p></div><form className="product-actions" onSubmit={inspect}><input type="url" required value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://example.com/product"/><button className="primary-button">{busy?"Inspecting...":"Inspect listing"}</button></form>{msg&&<div className="status-message">{msg}</div>}{data&&<section className="product-categories"><h2>Listing review</h2><div className="ocr-details-grid">{Object.entries(data).filter(([k])=>["productName","brand","mrp","currency","websiteName","url"].includes(k)).map(([k,v])=><div key={k}><strong>{k}</strong><span>{String(v||"Not recorded")}</span></div>)}</div><pre className="ocr-raw-text">{JSON.stringify(data,null,2)}</pre></section>}</div>}
