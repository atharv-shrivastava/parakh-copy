import {useEffect,useState} from "react";import {apiFetch} from "../lib/auth";export default function AdminRules(){const[rules,setRules]=useState([]),[msg,setMsg]=useState(""),[draft,setDraft]=useState({ruleId:"",ruleCode:"",ruleNumber:"",title:"",description:"",category:"General"});async function load(){try{const r=await apiFetch("http://localhost:5000/api/admin/rules");setRules(await r.json())}catch(e){setMsg(e.message)}}useEffect(()=>{load()},[]);async function add(e){e.preventDefault();const r=await apiFetch("http://localhost:5000/api/admin/rules",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...draft,definition:{}})});const d=await r.json();setMsg(r.ok?"Rule added":d.error);if(r.ok){setDraft({ruleId:"",ruleCode:"",ruleNumber:"",title:"",description:"",category:"General"});load()}}return <div className="products-page"><div className="page-header"><p className="eyebrow">ADMIN · COMPLIANCE RULES</p><h1>Compliance rules</h1></div>{msg&&<div className="status-message">{msg}</div>}<form className="product-actions" onSubmit={add}><input placeholder="Rule ID" required value={draft.ruleId} onChange={e=>setDraft({...draft,ruleId:e.target.value})}/><input placeholder="Rule code" required value={draft.ruleCode} onChange={e=>setDraft({...draft,ruleCode:e.target.value})}/><input placeholder="Rule number" required value={draft.ruleNumber} onChange={e=>setDraft({...draft,ruleNumber:e.target.value})}/><input placeholder="Title" required value={draft.title} onChange={e=>setDraft({...draft,title:e.target.value})}/><button className="primary-button">Add rule</button></form><div className="product-list">{rules.map(r=><div className="product-row" key={r.id}><div><strong>{r.ruleCode} · {r.ruleNumber}</strong><span>{r.title}</span></div><span>{r.enabled?"Enabled":"Disabled"}</span></div>)}</div></div>}
