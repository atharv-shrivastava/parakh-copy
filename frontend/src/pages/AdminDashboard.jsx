import {useEffect,useState} from "react";import {apiFetch} from "../lib/auth";export default function AdminDashboard(){const[d,setD]=useState(null),[e,setE]=useState("");useEffect(()=>{apiFetch("http://localhost:5000/api/admin/overview").then(async r=>{const x=await r.json();if(!r.ok)throw Error(x.error||"Admin overview unavailable");setD(x)}).catch(x=>setE(x.message))},[]);return <div className="products-page"><div className="page-header"><p className="eyebrow">ADMIN</p><h1>Admin dashboard</h1><p>System-wide inspection, product and rule activity.</p></div>{e&&<div className="status-message">{e}</div>}{d&&<div className="ocr-details-grid">{Object.entries(d.counts||{}).map(([k,v])=><div key={k}><strong>{k}</strong><span>{v}</span></div>)}</div>}</div>}
