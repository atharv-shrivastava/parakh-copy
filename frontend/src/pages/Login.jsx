import { useState } from "react";import { Link,useLocation,useNavigate } from "react-router-dom";import { saveSession,apiFetch } from "../lib/auth";import "../styles/auth.css";const API=import.meta.env.VITE_API_URL||"http://localhost:5000/api";export default function Login(){const[n,setN]=useState("");const[p,setP]=useState("");const[e,setE]=useState("");const nav=useNavigate();const loc=useLocation();async function submit(x){x.preventDefault();setE("");try{const r=await apiFetch(`${API}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:n,password:p})});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||"Login failed");saveSession(d.token,d.user);nav(loc.state?.from||"/",{replace:true});}catch(x){setE(x.message)}}return <div className="auth-page"><form className="auth-card" onSubmit={submit}><h1>PARAKH</h1><p>Sign in to inspect products and manage compliance records.</p>{e&&<div className="status-message">{e}</div>}<label>Email<input type="email" required value={n} onChange={x=>setN(x.target.value)}/></label><label>Password<input type="password" required value={p} onChange={x=>setP(x.target.value)}/></label><button className="primary-button">Sign in</button><Link to="/register">Create account</Link></form></div>}
