import { useState } from "react";import { Link,useNavigate } from "react-router-dom";import { saveSession,apiFetch } from "../lib/auth";import "../styles/auth.css";const API=import.meta.env.VITE_API_URL||"http://localhost:5000/api";export default function Register(){const[n,setN]=useState("");const[email,setEmail]=useState("");const[p,setP]=useState("");const[e,setE]=useState("");const nav=useNavigate();async function submit(x){x.preventDefault();setE("");try{const r=await apiFetch(`${API}/auth/register`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:n,email,password:p})});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||"Registration failed");const login=await apiFetch(`${API}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password:p})});const ld=await login.json();saveSession(ld.token,ld.user);nav("/",{replace:true});}catch(x){setE(x.message)}}return <div className="auth-page"><form className="auth-card" onSubmit={submit}><h1>Create PARAKH account</h1>{e&&<div className="status-message">{e}</div>}<label>Name<input required value={n} onChange={x=>setN(x.target.value)}/></label><label>Email<input type="email" required value={email} onChange={x=>setEmail(x.target.value)}/></label><label>Password<input type="password" minLength="6" required value={p} onChange={x=>setP(x.target.value)}/></label><button className="primary-button">Register</button><Link to="/login">Already have an account?</Link></form></div>}
