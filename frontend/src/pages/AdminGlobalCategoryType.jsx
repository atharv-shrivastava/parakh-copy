import {useParams} from "react-router-dom";import {Link} from "react-router-dom";export default function AdminGlobalCategoryType(){const{sourceType}=useParams();return <div className="products-page"><Link className="back-link" to="/admin/categories">← Global categories</Link><div className="page-header"><p className="eyebrow">ADMIN · {String(sourceType).toUpperCase()}</p><h1>Global {String(sourceType).toUpperCase()} categories</h1><p>Open a category from Global Categories to manage its hierarchy.</p></div></div>}
